## 포인터와 참조자
- 메모리 구조: 프로그램이 실행되기 위해서는 먼저 프로그램이 메모리에 로드(load)되어야 한다.
1. 코드영역(Code) - CPU는 코드영역에 저장된 명령어를 하나씩 가져가서 처리 -> 코드가 주문서처럼 저장되는 곳
2. 데이터영역(Data) - 프로그램 시작시 할당되며, 프로그램 종료시 소멸 -> 프로그램이 시작될 때부터 끝날 때까지 쭉 남아있는 데이터(전역 변수 등)
3. 스택영역(Stack) -LIFO: 가장 늦게 저장된 데이터가 가장 먼저 인출 -> 함수 안에서 잠깐 쓰고 버리는 변수(지역 변수)들이 머무는 곳입니다.
4. 힙영역(Heap) - 사용자에 의해 메모리 공간이 동적으로 할당되고 해제됨 -> 우리가 필요할때 'new'로 직접 땅을 빌리고 다 쓰면 'delete'로 하는 공간(동적 할당)
<img width="288" height="492" alt="image" src="https://github.com/user-attachments/assets/f79a944a-31db-4ef0-8f97-cd5658bcd279" />

### 포인터를 사용해야 하는 이유 : 관리와 절약!
**포인터를 사용함으로써 복사본을 만들지 않고 주소만 알려줌으로 해서 용량 및 처리시간에 이득을 볼 수 있다.**
- 메모리에 직접 접근이 가능하다.
- 구조화된 자료를 만들어서 효율적인 운영이 가능하다.
- Call by reference 방식의 이용이 가능하다.
- 배열, 구조체 등의 복잡한 자료 구조와 함수에 쉽게 접근할 수 있다.
- 메모리 동적 할당이 가능하다.  


★ 포인터(pointer) : 값이 저장된 장소의 주소를 저장하는 변수 ★  


★ 참조(reference) : 기존 변수에 새로운 이름을 붙여주는 것 ★
